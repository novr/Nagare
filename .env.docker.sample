# Docker環境用の環境変数設定サンプル
# 使用方法: このファイルを .env にコピーして、適切な値を設定してください
# cp .env.docker.sample .env

# =============================================================================
# データベース設定
# =============================================================================
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=nagare
DATABASE_USER=nagare_user
# パスワードは secrets/db_password.txt に記載してください（平文での設定は非推奨）
DATABASE_PASSWORD=change_this_password

# =============================================================================
# GitHub認証設定
# =============================================================================
# GitHub Apps認証（推奨）
GITHUB_APP_ID=123456
GITHUB_APP_INSTALLATION_ID=12345678
# Private Keyは改行を\nで表現
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END RSA PRIVATE KEY-----"

# または Personal Access Token（非推奨）
# GITHUB_TOKEN=your_personal_access_token_here

# =============================================================================
# アプリケーション設定
# =============================================================================
# モックモード（開発環境用）
# true: PostgreSQL未接続でもJSONから設定を読み込んで動作
# false: PostgreSQLから設定を読み込む（本番環境）
USE_DB_MOCK=false

# モックモード時の監視対象リポジトリ（JSON形式）
REPOSITORIES_JSON='[{"owner":"your-org","repo":"your-repo"}]'

# =============================================================================
# Airflow設定
# =============================================================================
# Webserver設定
AIRFLOW_WEBSERVER_PORT=8080

# 管理者アカウント（初回起動時に作成）
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin

# アラート通知先
AIRFLOW_ALERT_EMAIL=admin@example.com

# =============================================================================
# Superset設定
# =============================================================================
SUPERSET_PORT=8088

# =============================================================================
# セキュリティに関する注意事項
# =============================================================================
# 本番環境では以下のファイルに機密情報を配置してください：
# - secrets/db_password.txt          : データベースパスワード
# - secrets/airflow_secret_key.txt   : Airflow Secret Key
# - secrets/superset_secret_key.txt  : Superset Secret Key
#
# これらのファイルはGitにコミットしないでください（.gitignoreに含まれています）
