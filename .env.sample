# Nagare環境変数設定
#
# セットアップ手順:
# 1. cp .env.sample .env
# 2. ./scripts/setup-secrets.sh を実行（オプション、強力なパスワード自動生成）
# 3. .env を編集して必須項目を設定

# =============================================================================
# GitHub認証（必須）
# =============================================================================
# Personal Access Token（推奨）
# 権限: repo, read:org, workflow
GITHUB_TOKEN=

# GitHub Apps認証（エンタープライズ向け）
# GITHUB_APP_ID=
# GITHUB_APP_INSTALLATION_ID=
# GITHUB_APP_PRIVATE_KEY_PATH=/path/to/private-key.pem

# =============================================================================
# Airflow管理者設定（必須）
# =============================================================================
AIRFLOW_ADMIN_PASSWORD=

# =============================================================================
# データベースとシークレットキー（必須）
# =============================================================================
# setup-secrets.sh で自動生成するか、手動で強力なパスワードを設定
DATABASE_PASSWORD=
AIRFLOW_SECRET_KEY=
SUPERSET_SECRET_KEY=
